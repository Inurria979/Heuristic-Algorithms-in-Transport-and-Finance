[project]
name = "dtop"
version = "0.1.0"
description = "Transformers + reinforcement learning for the Dynamic Team Orienteering Problem"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
